/** @type {import('next').NextConfig} */
const nextConfig = {
  allowedDevOrigins: [
    'header-price-puts-translator.trycloudflare.com',
    '*.trycloudflare.com',
    '*.loca.lt',
  ],
};

export default nextConfig;
